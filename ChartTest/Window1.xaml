<Window x:Class="ChartTest.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:chart="clr-namespace:ag.WPF.Chart;assembly=ag.WPF.Chart"
        Title="Window1" Height="300" Width="300" Loaded="Window_Loaded"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid x:Name="grd">
        <chart:Chart x:Name="chrt" ItemsSource="{Binding Series}" Margin="24" SectionsX="60" AutoAdjust="False" AxesValuesFormat="0%" ChartStyle="Area" MaxY="100" AxesValuesVisibility="Vertical"/>
    </Grid>
</Window>
